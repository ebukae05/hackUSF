# Required for Gemini routing plans (without this, routing shows "Pending" but pipeline still runs)
GOOGLE_GENAI_USE_VERTEXAI=FALSE
GOOGLE_API_KEY=YOUR_GEMINI_API_KEY_HERE

# Optional — override when Flask backend is deployed to Cloud Run
# RELIEFLINK_BACKEND_URL=https://your-cloud-run-url.run.app
